pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'env'
                sh 'sudo su –'
                sh 'whoami'
            }
        }
    }
}
